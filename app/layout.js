export const metadata = { title: "Alex-IO", description: "HubSpot-native email bot." };
export default function RootLayout({ children }) {
  return (<html lang="en"><body className="min-h-screen">{children}</body></html>);
}