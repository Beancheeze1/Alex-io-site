// components/start-quote/constants.ts
//
// Shared constants for the two "start a quote" intake flows
// (StartQuoteModal.tsx and RepStartQuoteModal.tsx).

// Fit clearance subtracted from a complete-pack box's inside L/W to get the
// foam block's outside L/W (foam is cut slightly undersized so it fits).
export const FIT_ALLOW_IN = 0.125;
